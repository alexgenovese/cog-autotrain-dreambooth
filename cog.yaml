# Configuration for Cog ⚙️
# Reference: https://github.com/replicate/cog/blob/main/docs/yaml.md

build:
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  system_packages:
    - "libgl1-mesa-glx"
    - "ffmpeg"
    - "libsm6"
    - "libxext6"
  python_packages:
    - "autotrain-advanced==0.6.32"

  run:
    - autotrain setup 
    - curl -o /usr/local/bin/pget -L "https://github.com/replicate/pget/releases/download/v0.0.1/pget" && chmod +x /usr/local/bin/pget
    - git clone https://github.com/huggingface/autotrain-advanced
    
predict: "predict.py:Predictor"
image: "r8.im/alexgenovese/autotrain-dreambooth"
